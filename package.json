{
  "name": "ai-chat-qwen",
  "version": "1.0.0",
  "description": "AI Chat application with Qwen API",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run preview",
    "vercel-build": "cd frontend && npm install && npm run build && cp -r frontend/dist ."
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
